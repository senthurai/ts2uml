{
  "name": "ts2uml",
  "version": "0.0.54",
  "description": "typescript workflow to uml sequence",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "comment": " echo post-build is a custom script",
    "clean": "rimraf dist",
    "build": "npm run clean && tsc && sh post-build",
    "test": "jest",
    "publish": "npm run build && cd dist && npm publish",
    "uml-sprinkler": "uml-sprinkler src"
  },
  "keywords": [
    "ts",
    "uml"
  ],
  "author": "tin2tin",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.12.12",
    "rimraf": "^5.0.8",
    "typescript": "^5.5.3"
  }
}
