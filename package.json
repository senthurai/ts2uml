{
  "name": "ts2uml",
  "version": "0.0.20",
  "description": "typescript workflow to uml sequence",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "build": "tsc && terser -c -m -o ./out-dir/index.js -- ./dist/*.js   && post-build",
    "test": "jest",
    "uml-sprinkler": "uml-sprinkler src/test"
  },
  "keywords": [
    "ts",
    "uml"
  ],
  "author": "tin2tin",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.12.12",
    "terser": "^5.31.0"
  } 
}
